# La red corporativa bloquea registry.npmjs.org; usamos un espejo de solo
# lectura del registro oficial. Si Telefónica tiene un registro interno
# (Artifactory/Nexus), sustituir por esa URL.
registry=https://registry.npmmirror.com
